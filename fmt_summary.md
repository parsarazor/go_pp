# خلاصه جامع پکیج `fmt` در Go

این مستند نگاهی جامع به پکیج `fmt` دارد که یکی از پرکاربردترین پکیج‌های زبان Go برای ورودی/خروجی فرمت‌بندی شده است. ما این پکیج را از سه زاویه بررسی می‌کنیم: از بالا (کاربرد کلی)، از پایین (جزئیات فنی)، و از درون (مکانیسم‌های داخلی).

---

## ۱. نگاه از بالا (From Above) - کاربرد و هدف
**هدف:** پکیج `fmt` (مخفف format) وظیفه مدیریت ورودی و خروجی متن‌محور و فرمت‌بندی شده را بر عهده دارد. این پکیج شبیه به `printf` و `scanf` در زبان C عمل می‌کند اما با امنیت و سادگی بیشتر.

### افعال اصلی (Verbs)
عملیات اصلی این پکیج حول دو دسته تابع می‌چرخد:
*   **چاپ کردن (Printing):** برای نوشتن خروجی.
*   **اسکن کردن (Scanning):** برای خواندن ورودی.

### توابع پرکاربرد
*   `Print`, `Println`, `Printf`: چاپ در خروجی استاندارد (stdout).
*   `Sprint`, `Sprintln`, `Sprintf`: چاپ در یک رشته (String) و بازگرداندن آن.
*   `Fprint`, `Fprintln`, `Fprintf`: چاپ در هر چیزی که قابلیت نوشتن دارد (مثل فایل یا شبکه - `io.Writer`).
*   `Errorf`: فرمت‌بندی یک پیام خطا و بازگرداندن آن به عنوان `error`.

---

## ۲. نگاه از پایین (From Below) - جزئیات و سینتکس
در این سطح، ما با "چگونگی" فرمت‌بندی سروکار داریم. قدرت اصلی `fmt` در **Verbs** (افعال فرمت‌بندی) نهفته است که با `%` شروع می‌شوند.

### مهم‌ترین Verbs
*   **عمومی:**
    *   `%v`: مقدار را با فرمت پیش‌فرض چاپ می‌کند (هوشمندترین گزینه).
    *   `%+v`: برای structها، نام فیلدها را هم چاپ می‌کند.
    *   `%#v`: نمایش مقدار به صورت سینتکس Go (مناسب برای دیباگ).
    *   `%T`: نمایش نوع (Type) متغیر.
*   **رشته‌ها:** `%s` (رشته ساده)، `%q` (رشته کوتیشن‌دار).
*   **اعداد صحیح:** `%d` (ده‌دهی)، `%x` (هگز)، `%b` (باینری).
*   **اعداد اعشاری:** `%f` (اعشاری ساده)، `%e` (نماد علمی)، `%g` (هوشمندترین حالت بین f و e).
*   **بولین:** `%t`.
*   **اشاره‌گر:** `%p`.

### پرچم‌ها (Flags) و عرض (Width)
می‌توانید خروجی را دقیق‌تر کنترل کنید:
*   `%9.2f`: عرض کل ۹ کاراکتر، ۲ رقم اعشار.
*   `%-5s`: تراز کردن به چپ (Left-justify) با عرض ۵.
*   `%04d`: پر کردن با صفر (Padded with zeros) برای رسیدن به عرض ۴ (مثلاً `0012`).

---

## ۳. نگاه از درون (From Within) - مکانیسم‌ها و اینترفیس‌ها
چگونه `fmt` می‌داند هر چیزی را چطور چاپ کند؟ این کار از طریق **Reflection** و **Interfaceها** انجام می‌شود.

### اینترفیس‌های کلیدی
پکیج `fmt` برای تعامل با تایپ‌های سفارشی شما، اینترفیس‌های خاصی را تعریف می‌کند. اگر تایپ شما این متدها را داشته باشد، `fmt` از آن‌ها استفاده می‌کند:

1.  **`Stringer`**:
    ```go
    type Stringer interface {
        String() string
    }
    ```
    اگر متد `String()` را پیاده‌سازی کنید، `Print` و `%v` و `%s` از خروجی این متد استفاده می‌کنند. این رایج‌ترین راه برای شخصی‌سازی نمایش آبجکت‌هاست.

2.  **`Formatter`**:
    ```go
    type Formatter interface {
        Format(f State, verb rune)
    }
    ```
    برای کنترل کامل و پیشرفته. شما می‌توانید دقیقاً تعیین کنید که اگر کاربر `%f` زد چه شود، یا اگر `%v` زد چه شود.

3.  **`GoStringer`**:
    ```go
    type GoStringer interface {
        GoString() string
    }
    ```
    مخصوص Verb `%#v`. برای زمانی که می‌خواهید نمایش دیباگ (Go-syntax) را شخصی‌سازی کنید.

### مدیریت خطا (Error Handling)
اگر آرگومان‌ها با فرمت همخوانی نداشته باشند (مثلاً پاس دادن `string` به `%d`)، پکیج `fmt` پنیک نمی‌کند، بلکه یک پیام خطا داخل خروجی چاپ می‌کند (مثلاً `%!d(string=hello)`).

---

### جمع‌بندی
*   **بالا:** ابزاری برای I/O استاندارد و فرمت‌بندی متن.
*   **پایین:** مجموعه‌ای غنی از Verbs (`%v`, `%d`, ...) برای کنترل دقیق نمایش داده‌ها.
*   **درون:** استفاده از Reflection و اینترفیس `Stringer` برای انعطاف‌پذیری و توسعه‌پذیری.
